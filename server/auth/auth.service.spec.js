'use strict';

var should = require('should');
var auth = require('./auth.service')

describe("Auth service", function() {

  xit('should validate a valid session');

  xit('should invalidate an invalid session');

});
